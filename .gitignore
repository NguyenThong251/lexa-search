# Build output
/dist/
*.zip

# Composer (the plugin ships its own PSR-4 autoloader in bootstrap.php;
# there is no composer install step)
/vendor/
composer.lock

# Local/editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Never commit credentials. A GitHub token (only needed to raise the API rate
# limit now the repo is public) belongs in wp-config.php as LEXA_GITHUB_TOKEN,
# never in the plugin tree.
.env
.env.*
*.key
*.pem

# Internal roadmap / product planning. Deliberately untracked: this repo is
# public, and PLAN.md carries commercial positioning and the Pro roadmap.
/PLAN.md
